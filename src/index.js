import React from "react"; //Librairie
import ReactDOM from "react-dom/client"; //Librairie
import App from "./App.js"; //Composant (Component)
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
